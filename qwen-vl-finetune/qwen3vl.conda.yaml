name: qwen3vl

channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - --extra-index-url https://download.pytorch.org/whl/cu126
    - torch==2.6.0
    - torchvision==0.21.0
    - transformers==4.57.3
    - deepspeed==0.17.1
    # need install after, with --no-build-isolation
    # - flash_attn==2.7.4.post1
    - triton==3.2.0
    - accelerate==1.7.0
    - torchcodec==0.2
    - peft==0.17.1
    - ninja
    - packaging
    - tensorboard
